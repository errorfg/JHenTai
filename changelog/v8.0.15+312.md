- Merge upstream cookie login feature with webview-assisted login and iOS fixes
- Add auto sync trigger on new history record (every 5 new records)
- Fix CI: detect tag at publish time instead of triggering on tag push
