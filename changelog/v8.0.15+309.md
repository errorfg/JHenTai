- 修复 README 多语言跳转链接
  - 英文/中文/韩文 README 顶部语言切换改为仓库内相对链接，避免跳转回上游仓库
- 修复搜索记录云同步合并逻辑
  - 不再按整列表“新旧覆盖”，改为双端去重合并并保留前 50 条，避免新搜索被旧顺序冲掉
  - 兼容旧格式搜索记录并修复刷新时可能的累积问题
- CI 触发逻辑优化
  - `Build & Release` 仅在 `v*` tag（或手动）触发，避免同一提交在 `master` 与 tag 上重复构建

--------------------

- Fix README language-switch links
  - Switch top language links in EN/CN/KR READMEs to repo-relative paths so navigation does not jump back to upstream
- Fix cloud sync merge behavior for search history
  - Replace full-list overwrite with deduplicated two-side merge (top 50), preventing newer searches from being overwritten by older ordering
  - Add compatibility for legacy search-history format and avoid accumulation issues on refresh
- Optimize CI trigger logic
  - Run `Build & Release` only on `v*` tags (or manual dispatch) to prevent duplicate builds from both `master` push and tag push
