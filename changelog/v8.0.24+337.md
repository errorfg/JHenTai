- 云同步架构升级：历史记录与阅读进度改为增量同步（oplog + 快照），日常同步只传输变化部分，不再随数据量增长变慢
- 修复多设备并发同步互相覆盖导致的同步丢失问题
- 修复同步合并可能回滚正在阅读的最新进度的问题（时间戳守卫写回，SQLite 层原子保护）
- 同步时间戳统一为 UTC 固定宽度格式，修复跨时区/时钟偏差下的合并错乱，存量数据自动迁移
- 同步全程加互斥锁；内容无变化时跳过上传；同步数据 gzip 压缩
- 首次升级后各设备自动完成 v2 格式迁移，过渡期与旧版本客户端兼容
