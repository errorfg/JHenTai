- 新增热门页面过滤器功能
  - 添加过滤器按钮到热门页面 AppBar
  - 支持客户端过滤，提供与搜索页面相同的过滤能力
  - 支持按分类、页数范围、评分、语言、关键词、标签等多维度过滤
  - 过滤配置自动保存，下次打开时恢复
- 新增排名页面过滤器功能
  - 添加过滤器按钮到排名页面 AppBar（位于排名类型选择器前）
  - 复用热门页面的客户端过滤逻辑
  - 支持对排名前 200 的画廊进行筛选
  - 支持日榜、月榜、年榜、总榜的独立过滤配置
- 两个页面均支持以下过滤器
  - 分类过滤（10 种类别：同人志、漫画、CG 等）
  - 页面范围（最少/最多页数）
  - 最低评分（1-5 星）
  - 语言过滤
  - 关键词搜索（在标题中搜索）
  - 标签过滤（支持命名空间，AND 逻辑）
  - 已删除画廊过滤

--------------------

- Add filter feature to Popular (Trending) page
  - Add filter button to Popular page AppBar
  - Support client-side filtering with same capabilities as search page
  - Filter by category, page range, rating, language, keywords, tags, and more
  - Filter configuration auto-saved and restored on next visit
- Add filter feature to Ranklist page
  - Add filter button to Ranklist page AppBar (before ranking type selector)
  - Reuse Popular page's client-side filtering logic
  - Filter top 200 ranked galleries
  - Support independent filter configuration for daily, monthly, yearly, and all-time rankings
- Both pages support the following filters
  - Category filtering (10 categories: Doujinshi, Manga, CG, etc.)
  - Page range (minimum/maximum pages)
  - Minimum rating (1-5 stars)
  - Language filtering
  - Keyword search (search in titles)
  - Tag filtering (with namespace support, AND logic)
  - Expunged galleries filtering
