- 新增同步设置导出
  - 将同步开关配置纳入配置导出项，便于多设备保持一致
- 新增 nhentai 备选支持（轻量模式）
  - 搜索/详情/图片页链路支持 nhentai
  - 搜索页新增 NH 切换按钮（`nh:` 模式）
  - 支持 nhentai 链接识别与跳转（含剪贴板检测场景）
  - 详情页新增 EH 按钮，可从 nh 标题提取关键词回搜 E-Hentai
- CI 流程调整
  - 分支检查聚焦 Android release APK 产物
  - 支持 docs-only 变更跳过编译
  - 支持 `[skip-build]` 关键词跳过编译任务

--------------------

- Add sync setting export
  - Include sync toggles in exported configuration for better multi-device consistency
- Add lightweight nhentai fallback support
  - Support nhentai search/detail/image flow
  - Add `NH` toggle in search page (`nh:` mode)
  - Support nhentai URL recognition and jump (including clipboard detection entry)
  - Add `EH` action in nhentai detail page to extract title keywords and search back on E-Hentai
- CI workflow updates
  - Branch checks now focus on Android release APK artifact output
  - Docs-only changes can skip build
  - `[skip-build]` keyword support to skip build jobs
